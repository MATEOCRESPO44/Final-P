import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    resizeMode: "contain",
    height: 150,
    width: "100%",
    marginTop: 20,
  },
  content: {
    marginHorizontal: 40,
    //backgroundColor: "#32cd32",
  },
});
